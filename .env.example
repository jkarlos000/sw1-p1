# Configuración de Base de Datos
POSTGRES_DB=sw1_database
POSTGRES_USER=sw1_user
POSTGRES_PASSWORD=your_secure_password_here

# CORS Configuration
CORS_ORIGIN=*

# Dominio (para SSL)
DOMAIN=localhost

# Puerto del Backend (opcional, por defecto 3000)
# PORT=3000
